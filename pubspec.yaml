name: the_afterglow_diaries
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hooks_riverpod: ^1.0.0-dev.11
  http: ^0.13.4
  flutter_dotenv: ^5.0.2
  cached_network_image: ^3.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.4
  freezed: ^1.1.1
flutter:
  uses-material-design: true
  assets:
    - production.env